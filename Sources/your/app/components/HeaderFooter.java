package your.app.components;

import com.webobjects.appserver.WOContext;
import com.webobjects.appserver.WOComponent;

public class HeaderFooter extends BaseComponent {
    public HeaderFooter(WOContext context) {
        super(context);
    }
}